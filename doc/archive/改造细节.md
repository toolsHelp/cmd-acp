这个结果基本把整个架构方向定死了。

结论先说：

**可以继续做方案 1，但目的需要调整。**

现在不是为了验证“Paseo 有没有权限能力”（已经证明有），而是为了拿到：

1. **Paseo 启动自定义 ACP Agent 时的真实 initialize 流程**
2. **Paseo 给 ACP Agent 的 client capabilities**
3. **自定义 cmd-acp 是否只需要替换 command 字段即可接入**

也就是说，下一步验证的是：

> `cmd-acp fork` 是否可以作为 Paseo 原生 ACP provider 工作，而不需要修改 Paseo。

---

## 我建议执行方案 1，但不要伪装长期 provider

你的探针方案：

```
PATH 前置

codebuddy-code.exe

        |
        |
        v

Paseo spawn

        |
        |
        v

ACP probe

记录:

initialize
session/new
session/update
session/request_permission
```

这个方向正确。

但是我建议不要覆盖真实：

```
codebuddy-code
```

而是做一个临时 shim：

```text
C:\acp-probe\

    codebuddy-code.cmd

```

内容：

```bat
@echo off

node C:\acp-probe\probe.js

```

PATH：

```
C:\acp-probe
C:\Program Files\...
```

优先级：

```
probe
 ↓
real executable
```

这样：

* 不破坏 Paseo
* 不污染安装目录
* 随时删除

---

# 需要抓的不是全部日志，而是 ACP 原始帧

probe 需要实现：

stdin:

```json
{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}
```

stdout:

必须保持 JSON-RPC。

所以：

## stdout

只能输出 ACP 回复。

例如：

```json
{
 "jsonrpc":"2.0",
 "id":1,
 "result":{
 }
}
```

## stderr

输出审计：

```
initialize params:

session/new params:

session/update params:
```

否则 Paseo 会认为协议污染。

---

# 重点抓 4 个节点

## 1. initialize

目标：

确认：

```json
{
 capabilities:{
 }
}
```

尤其：

```json
clientCapabilities
```

结构。

---

## 2. session/new

重点：

Paseo 是否传：

```json
{
 sessionId,
 cwd,
 mcpServers,
 configOptions
}
```

因为 cmd-acp 当前：

```ts
session/new

↓

落 .mcp.json

↓

spawn cmd
```

这里可能需要同步。

---

## 3. tool_call update

这是你发现的 C1。

必须确认顺序：

Paseo 实际期望：

```
session/update

{
 update:{
   sessionUpdate:"tool_call",
   toolCallId:"xxx",
   status:"in_progress"
 }
}


↓

session/request_permission


↓

session/update

{
 status:"completed"
}

```

而不是：

```
permission
 |
tool
```

---

## 4. permission response

确认：

Paseo 返回：

```json
{
 outcome:{
    outcome:"selected",
    optionId:"allow-once"
 }
}
```

还是：

```json
{
 optionId:"allow_once"
}
```

这个影响 broker 映射。

---

# 根据你现在的发现，cmd-acp 需要增加一个状态机

之前设计：

```
request
 |
wait
 |
response
```

太简单。

Paseo 实际需要：

```
ToolCall State Machine
```

建议：

```text
ToolCall

created

   |
   |
pending

   |
   |
in_progress

   |
   +----------------+
   |                |
permission       execute
required            |
   |                |
   v                |
waiting             |
   |                |
allow/deny          |
   |                |
   +----------------+
          |
          v

completed
or failed
```

---

# 所以 cmd-acp 端最终新增两个 Store

不是一个。

## ToolCallStore

管理：

```ts
Map<
 toolCallId,
 ToolCallState
>
```

例如：

```ts
{
 id:"sess1-tool-001",

 kind:"execute",

 title:"npm install",

 status:"pending"
}
```

---

## PermissionStore

管理：

```ts
Map<
 requestId,
 PendingPermission
>
```

关系：

```
toolCallId
     |
     |
permissionRequest
     |
     |
requestId

```

不能混。

---

# 最终 cmd-acp 架构应该变成

```
                 ACP Client
                    |
                    |
              session/update
                    |
                    |
              ToolCallStore
                    |
                    |
command-code ---- PermissionBroker
                    |
                    |
              PermissionStore
                    |
                    |
          session/request_permission

```

---

# 关于你提到的 MCP 自动批准

这个发现也很重要：

> Paseo 有 mcp__paseo__respond_to_permission

说明：

Paseo 的权限系统已经抽象成：

```
Permission Request
        |
        |
Provider
        |
        |
UI / MCP / Automation
```

所以 cmd-acp 不应该自己实现：

* 弹窗
* confirm
* CLI 输入

只需要：

```text
把 permission request 正确发给 ACP Client
```

剩下交给 Paseo。

---

# 当前优先级重新排序

根据最新证据：

## P0

### cmd-acp

修：

1. toolCall 生命周期
2. toolCallId 稳定化
3. permission request bridge

### command-code

增加：

4. permission broker

---

## P1

initialize capability 探测。

重要，但不是阻塞。

---

## P2

loadSession / pending permission 持久化。

---

所以我的建议：

**执行方案 1。**

但探针不要叫“替换 codebuddy-code”，而是做一个 **ACP Trace Shim**。

拿到：

```
initialize
session/new
session/update
request_permission
```

四组真实 JSON 后，就可以把 `cmd-acp` 的改造直接落到代码级。

下一步输出应该是：

```
Paseo ACP Trace Shim 设计
+
probe.js 完整代码
+
如何安全捕获 stdio JSON-RPC
```

这个探针本身以后还能作为 ACP 调试工具保留下来。
